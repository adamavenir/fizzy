<% if card.is_a?(BeadsIssue) %>
  <%= turbo_frame_tag card, :columns do %>
    <%= render "beads/issues/columns/buttons", card: card %>
  <% end %>
<% else %>
  <%= turbo_frame_tag card, :columns, src: edit_card_column_path(card), target: "_top", refresh: "morph" %>
<% end %>
